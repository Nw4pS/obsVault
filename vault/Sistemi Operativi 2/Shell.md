La Shell è un programma che esegue altri programmi ([[comandi]]) detta "interprete di comandi"
In questi appunti viene utilizzata Bash
Una volta aperta si vede l'utente e il [[percorso]] , nel caso sia nella home, si legge ~ detta "tilde".
