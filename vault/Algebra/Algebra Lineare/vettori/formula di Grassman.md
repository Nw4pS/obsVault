dato uno spazio vettoriale W su K e due spazi vettoriali $U,V \subset W$ e le seguenti operazioni su essi:
- $U + V := {u+v | u \in U, v \in V}$
- $U \cap V := {w | w \in U, w \in V}$
allora:
$$dim(U+V)=dim(U)+dim(V)-dim(U \cap V)$$
questo perché due sottospazi figli di V possono avere dimensione e quindi basi diverse da esso, basta che non lo superino, in un mondo 3D possono esistere elementi 2D o 1D, ma non 4D.